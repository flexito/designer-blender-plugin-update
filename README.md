This project is depending on blender python files. User can commit with below step:

##

git add files

##

git commit -m "commit message"

##

git tag -a v0.1 -m "version 0.1"

##

git push origin master --tag
